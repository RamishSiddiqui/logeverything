# Core dependencies
# LogEverything requires no external dependencies for its core functionality

# Development dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
ruff>=0.1.6
mypy>=0.971

# Documentation dependencies
sphinx>=5.0.0
furo>=2023.3.27
sphinx-autodoc-typehints>=1.18.0
myst-parser>=1.0.0  # For rendering markdown in Sphinx documentation

# Build and packaging dependencies
build>=0.8.0
twine>=4.0.0
wheel>=0.38.0
pytest-xdist>=3.0.0

# Code quality dependencies
bandit>=1.7.0
pre-commit>=3.3.2  # For managing Git hooks

# Integration testing dependencies
langchain>=0.0.313
fastapi>=0.104.1
mlflow>=2.8.1
transformers[torch]>=4.35.2
pandas>=2.1.3
numpy>=1.26.2
requests>=2.31.0
uvicorn>=0.24.0
scikit-learn>=1.3.2  # Required for MLflow example

# Optional monitoring system dependencies
# These are required for the enhanced monitoring and dashboard features
websockets>=11.0.0  # For real-time WebSocket streaming
aiohttp>=3.8.0  # For HTTP dashboard server
aiohttp-cors>=0.7.0  # For CORS support in dashboard
tomli>=2.0.0  # For TOML configuration file support
psutil>=5.9.0  # For system metrics collection
fastapi>=0.100.0  # For monitoring API server (optional)
uvicorn>=0.20.0  # For running FastAPI server (optional)
